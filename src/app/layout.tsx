export const metadata = {
  title: 'Grocery AI API',
  description: 'API for grocery shopping app',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}